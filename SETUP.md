# 🐾 Pet Tashkent - Шахар ҳайвонлар экотизими

Полнофункциональный веб-сайт для поиска, продажи и адопции питомцев в Ташкенте.

## 📋 Функциональность

### Frontend (React + Tailwind CSS)
- ✅ Главная страница с информацией о сервисе
- ✅ Каталог животных с фильтрацией по виду и возрасту
- ✅ Поиск по названию, породе, описанию
- ✅ Страница животного с полной информацией
- ✅ Встроенный чат между покупателем и продавцом
- ✅ Форма подачи объявления о животном
- ✅ Справочник приютов и ветеринарных клиник
- ✅ Система жалоб и модерации
- ✅ Авторизация (пользователь/админ)

### Backend (json-server)
- ✅ RESTful API для всех операций
- ✅ Примеры данных в db.json
- ✅ Поддержка CRUD операций

### Админ-панель
- ✅ Просмотр всех объявлений
- ✅ Подтверждение/отклонение объявлений
- ✅ Управление пользователями
- ✅ Просмотр и обработка жалоб

## 🚀 Быстрый старт

### 1. Установка зависимостей
```bash
npm install
```

### 2. Запуск json-server (в отдельном терминале)
```bash
npm run server
```
Сервер будет запущен на `http://localhost:3001`

### 3. Запуск приложения React (в другом терминале)
```bash
npm run dev
```
Приложение будет доступно на `http://localhost:5173`

## 📝 Демо учетные данные

### Обычный пользователь
- Email: `aziz@example.com`
- Пароль: `$2a$10$YjNpbHNjcGlkbg==`

### Администратор
- Email: `admin@example.com`
- Пароль: `$2a$10$YWRtaW5wYXNz`

## 📁 Структура проекта

```
src/
├── components/          # React компоненты
│   ├── ChatComponent.jsx
│   ├── Footer.jsx
│   ├── Navbar.jsx
│   ├── PetCard.jsx
│   ├── ProtectedRoute.jsx
│   ├── Sidebar.jsx
│   └── ui/             # UI компоненты
│       ├── Button.jsx
│       ├── Card.jsx
│       ├── Input.jsx
│       ├── Modal.jsx
│       ├── Spinner.jsx
│       └── Toast.jsx
├── pages/              # Страницы
│   ├── HomePage.jsx
│   ├── PetsPage.jsx
│   ├── PetDetailPage.jsx
│   ├── CreateAdPage.jsx
│   ├── SheltersPage.jsx
│   ├── admin/
│   │   └── AdminDashboard.jsx
│   └── auth/
│       └── LoginPage.jsx
├── services/           # API сервисы
│   └── petService.js
├── hooks/              # Custom hooks
├── contexts/           # React contexts
├── store/              # Redux store
└── utils/              # Утилиты

db.json                # База данных json-server
```

## 🔌 API маршруты

### Животные
```
GET /pets              - Получить всех животных
GET /pets/:id         - Получить одного животного
POST /pets            - Создать животное
PATCH /pets/:id       - Обновить животное
DELETE /pets/:id      - Удалить животное
```

### Пользователи
```
GET /users            - Получить всех пользователей
GET /users/:id        - Получить пользователя
POST /users           - Создать пользователя
```

### Сообщения
```
GET /messages         - Получить сообщения
POST /messages        - Отправить сообщение
PATCH /messages/:id   - Отметить как прочитанное
```

### Жалобы
```
GET /reports          - Получить жалобы
POST /reports         - Создать жалобу
PATCH /reports/:id    - Обновить статус жалобы
```

### Приюты
```
GET /shelters         - Получить приюты
GET /shelters/:id     - Получить приют
```

## 🎨 Дизайн и стиль

- **Framework**: Tailwind CSS
- **Иконки**: Lucide React (500+ иконок)
- **Цветовая схема**: Синяя и зелёная палитра
- **Адаптивность**: Mobile-first подход
- **Тема**: Зоо-тематика (🐾, 🐕, 🐈, и т.д.)

## 📦 Использованные библиотеки

```json
{
  "react": "^19.2.0",
  "react-dom": "^19.2.0",
  "react-router-dom": "^6.26.2",
  "lucide-react": "^0.556.0",
  "json-server": "^1.0.0-beta.3"
}
```

## 🌟 Основные компоненты

### PetCard
Карточка животного с изображением, информацией и кнопками действия.

```jsx
<PetCard pet={pet} onReport={handleReport} />
```

### ChatComponent
Встроенный чат для общения между пользователями.

```jsx
<ChatComponent 
  currentUserId={1}
  otherUserId={2}
  petId={1}
  petName="Шарик"
  ownerName="Азиз"
/>
```

### PetDetailPage
Полная информация о животном с фото, описанием и чатом.

### AdminDashboard
Админ-панель для управления объявлениями, пользователями и жалобами.

## 📝 Примеры использования API

### Получить всех животных с фильтром
```javascript
const pets = await petService.getAllPets({ 
  type: 'Собака',
  minAge: 1,
  maxAge: 5
});
```

### Создать новое объявление
```javascript
const pet = await petService.createPet({
  name: 'Шарик',
  type: 'Собака',
  breed: 'Лабрадор',
  age: 3,
  description: 'Веселый щенок',
  owner_id: 1,
  owner_name: 'Азиз',
  owner_phone: '+998 90 123-45-67',
  location: 'Ташкент'
});
```

### Отправить сообщение
```javascript
const message = await messageService.sendMessage({
  sender_id: 1,
  receiver_id: 2,
  pet_id: 1,
  content: 'Привет, интересует твой питомец!'
});
```

### Подать жалобу
```javascript
const report = await reportService.createReport({
  pet_id: 1,
  user_id: 2,
  reason: 'Мошенничество',
  description: 'Фото не соответствует действительности'
});
```

## 🔐 Функции безопасности

1. **Модерация объявлений** - Все новые объявления требуют одобрения админа
2. **Система жалоб** - Пользователи могут жаловаться на мошенников
3. **Авторизация** - Разделение прав пользователя и админа
4. **Рейтинг пользователей** - Система оценки продавцов

## 🗺️ Карта и местоположение

- Интегрирована Google Maps для отображения приютов и ветеринарных клиник
- Координаты всех учреждений сохранены в db.json
- Возможность открытия маршрута в Google Maps

## 📞 Контакты

Для вопросов и предложений:
- Email: info@pettashkent.uz
- Phone: +998 71 234 56 78
- Telegram: @pettashkent

## 📄 Лицензия

MIT License - свободное использование в образовательных целях

---

**Сделано с ❤️ для любителей животных в Ташкенте!**
