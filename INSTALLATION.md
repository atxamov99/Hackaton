# 🐾 Pet Tashkent - Веб-сайт для поиска и адопции питомцев

**Полнофункциональное приложение React + json-server для управления объявлениями о животных в Ташкенте**

---

## 🌟 Основные возможности

### 📋 Каталог животных
- ✅ Карточки всех животных с фото и информацией
- ✅ Поиск по имени, породе и описанию
- ✅ Фильтрация по виду животного и возрасту
- ✅ Детальная страница каждого животного

### 💬 Система чата
- ✅ Встроенный чат между покупателем и продавцом
- ✅ Выбор конкретного животного при переписке
- ✅ История сообщений

### 📝 Управление объявлениями
- ✅ Форма подачи нового объявления о животном
- ✅ Загрузка фото, описания, информации об владельце
- ✅ Модерация и одобрение объявлений

### 🏥 Справочник приютов
- ✅ Список всех приютов и ветеринарных клиник
- ✅ Контактная информация и веб-сайты
- ✅ Интеграция с Google Maps

### 🛡️ Система безопасности
- ✅ Авторизация (пользователь/админ)
- ✅ Система жалоб на мошенников
- ✅ Рейтинг продавцов

### ⚙️ Админ-панель
- ✅ Управление объявлениями (одобрение/отклонение)
- ✅ Управление пользователями
- ✅ Рассмотрение жалоб
- ✅ Статистика

---

## 📦 Установка и запуск

### 1️⃣ Установите зависимости
```bash
npm install
```

### 2️⃣ Запустите json-server в отдельном терминале
```bash
npm run server
```
**Результат:**
```
Resources
  http://localhost:3001/pets
  http://localhost:3001/users
  http://localhost:3001/messages
  http://localhost:3001/reports
  http://localhost:3001/shelters

Home
  http://localhost:3001
```

### 3️⃣ Запустите React приложение в другом терминале
```bash
npm run dev
```

**Приложение откроется на:** `http://localhost:5173`

### ⚡ Или используйте стартовый скрипт (Windows)
```bash
.\start.bat
```

### 🐧 Или для Mac/Linux
```bash
bash start.sh
```

---

## 🔐 Демо учетные данные

### 👤 Обычный пользователь
```
Email: aziz@example.com
Пароль: $2a$10$YjNpbHNjcGlkbg==
```

### 👑 Администратор
```
Email: admin@example.com
Пароль: $2a$10$YWRtaW5wYXNz
```

---

## 📁 Структура проекта

```
src/
├── pages/                      # Основные страницы
│   ├── HomePage.jsx           # Главная страница
│   ├── PetsPage.jsx          # Каталог животных
│   ├── PetDetailPage.jsx     # Страница животного
│   ├── CreateAdPage.jsx      # Создание объявления
│   ├── SheltersPage.jsx      # Приюты и клиники
│   ├── auth/
│   │   └── LoginPage.jsx     # Авторизация
│   └── admin/
│       └── AdminDashboard.jsx # Админ панель
│
├── components/                 # React компоненты
│   ├── Navbar.jsx            # Навигация
│   ├── Footer.jsx            # Подвал
│   ├── PetCard.jsx           # Карточка животного
│   ├── ChatComponent.jsx     # Чат
│   ├── ProtectedRoute.jsx    # Защищенный маршрут
│   ├── Sidebar.jsx           # Боковая панель
│   └── ui/                   # UI компоненты
│       ├── Button.jsx
│       ├── Card.jsx
│       ├── Input.jsx
│       ├── Modal.jsx
│       ├── Spinner.jsx
│       └── Toast.jsx
│
├── services/
│   └── petService.js         # API сервис
│
├── hooks/                      # Custom React hooks
├── contexts/                   # React Context API
├── store/                      # Redux store (опционально)
├── utils/                      # Утилиты и константы
│
├── App.jsx                    # Главное приложение
├── main.jsx                   # Точка входа
├── index.css                  # Tailwind CSS
│
└── API_EXAMPLES.js            # Примеры использования API

db.json                        # База данных json-server
package.json                   # Зависимости
```

---

## 🔌 API Структура

### Животные (`/pets`)
```javascript
GET    /pets                    // Получить всех
GET    /pets/:id              // Получить одно
POST   /pets                  // Создать
PATCH  /pets/:id              // Обновить
DELETE /pets/:id              // Удалить
```

### Пользователи (`/users`)
```javascript
GET    /users                 // Получить всех
GET    /users/:id            // Получить одного
POST   /users                // Регистрация
```

### Сообщения (`/messages`)
```javascript
GET    /messages              // Получить все
POST   /messages              // Отправить
PATCH  /messages/:id         // Отметить как прочитано
```

### Жалобы (`/reports`)
```javascript
GET    /reports               // Получить все
POST   /reports               // Создать
PATCH  /reports/:id          // Обновить статус
```

### Приюты (`/shelters`)
```javascript
GET    /shelters              // Получить все
GET    /shelters/:id         // Получить один
```

---

## 📖 Примеры использования API

### Получить всех животных
```javascript
import { petService } from './services/petService';

const pets = await petService.getAllPets();
```

### Создать животное
```javascript
const newPet = await petService.createPet({
  name: 'Барсик',
  type: 'Кошка',
  breed: 'Сиамская',
  age: 2,
  description: 'Красивая кошка...',
  owner_name: 'Сергей',
  owner_phone: '+998 90 123-45-67',
  location: 'Ташкент'
});
```

### Отправить сообщение
```javascript
const message = await messageService.sendMessage({
  sender_id: 1,
  receiver_id: 2,
  pet_id: 1,
  content: 'Привет! Интересует твой питомец'
});
```

### Подать жалобу
```javascript
const report = await reportService.createReport({
  pet_id: 1,
  user_id: 2,
  reason: 'Мошенничество',
  description: 'Фото не соответствует...'
});
```

**Больше примеров в файле `src/API_EXAMPLES.js`**

---

## 🎨 Дизайн и стиль

- **Framework:** Tailwind CSS
- **Иконки:** Lucide React (500+ иконок)
- **Цветовая схема:** Синяя/Зелёная палитра
- **Адаптивность:** Mobile-first подход
- **Тема:** Зоо-тематика 🐾🐕🐈

---

## 🚀 Развертывание

### Для продакшена
```bash
npm run build
```

Это создаст оптимизированную сборку в папке `dist/`.

### Хостирование
- Frontend можно разместить на Vercel, Netlify, GitHub Pages
- Backend можно развернуть на Heroku, Railway, Render

---

## 📚 Используемые технологии

```json
{
  "react": "^19.2.0",
  "react-dom": "^19.2.0",
  "react-router-dom": "^6.26.2",
  "lucide-react": "^0.556.0",
  "tailwindcss": "^3.0.0",
  "json-server": "^1.0.0-beta.3",
  "vite": "^5.0.0"
}
```

---

## 🐛 Решение проблем

### Ошибка: "Cannot GET /pets"
- Убедитесь, что json-server запущен на порту 3001
- Проверьте `db.json` в корневой папке проекта

### Ошибка: "Port 3001 already in use"
- Закройте предыдущий процесс json-server
- Или используйте другой порт: `npm run server -- --port 3002`

### Ошибка: "Module not found"
- Переустановите зависимости: `npm install`
- Очистите кэш: `npm cache clean --force`

---

## 📝 Особенности

### Примеры данных в db.json
- 5 животных
- 6 пользователей (включая админа)
- 3 сообщения
- 1 жалоба
- 4 приюта/клиники

### Возможность расширения
- Добавьте больше данных в `db.json`
- Создавайте новые страницы и компоненты
- Расширяйте API сервис
- Добавляйте новые фильтры и функции

---

## 👨‍💼 Администрирование

### Функции админа
1. **Просмотр объявлений** - Модерирование всех объявлений
2. **Одобрение/Отклонение** - Контроль качества контента
3. **Управление пользователями** - Удаление мошенников
4. **Рассмотрение жалоб** - Защита от спама и злоупотреблений

---

## 📞 Поддержка

Для вопросов и предложений:
- 📧 Email: info@pettashkent.uz
- 📱 Telegram: @pettashkent
- 🌐 Website: www.pettashkent.uz

---

## 📄 Лицензия

MIT License - Свободное использование в образовательных целях

---

## 🎯 Будущие улучшения

- [ ] Интеграция с платежными системами
- [ ] Система оценок и отзывов
- [ ] Push-уведомления
- [ ] Загрузка фото в облако
- [ ] SMS подтверждение номера
- [ ] Расширенная аналитика
- [ ] Мобильное приложение
- [ ] Видео-консультации с ветеринаром

---

**Сделано с ❤️ для любителей животных в Ташкенте!**

🐾 **Pet Tashkent - Шахар ҳайвонлар экотизими** 🐾
